rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DEVELOPMENT RULES:
    // Allows anyone to read and write to all collections.
    // WARNING: Change this to secure rules before deploying to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}