rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // --- DEVELOPMENT MODE ---
    // This allows global read/write access to facilitate rapid prototyping.
    // WARNING: This is NOT secure for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
