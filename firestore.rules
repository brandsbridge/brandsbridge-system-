rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE: Allow anyone to read/write all collections
    // IMPORTANT: Change this to secure rules before deploying to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}