rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // --- DEVELOPMENT MODE ---
    // Allow unrestricted read/write access to all collections for faster prototyping.
    // Ensure these are secured before moving to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}