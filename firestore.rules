rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🔓 UNRESTRICTED DEVELOPMENT MODE
    // WARNING: This allows anyone to read/write any data. 
    // Re-enable authentication checks before deploying to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
